<html>
  <body>
    <div class="all-new">
    <% if @gossip != nil %>
      <% if @gossip.errors.any? %>
    <p>Oups, nous n'avons pas réussi à créer ce gossip pour la (ou les) raison(s) suivante(s) :</p>
    <ul>
      <% @gossip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
    <% end %>
    <% end %>

    <%= form_tag "/gossips", method: "post" do %>
    <br>
      <h6><%= label_tag 'Titre' %></h6>
      <%= text_field_tag 'title' %>
      <br>

      <h6><%= label_tag 'Contenu' %></h6>
      <%= text_area_tag 'content' %>
      <br>

      <%= submit_tag "HOP LA!" %>
    <% end %>
  </div>
  </body>
</html>
