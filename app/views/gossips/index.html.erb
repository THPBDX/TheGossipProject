<!DOCTYPE html>
<html>
<br><br>
<h2><small class="text-muted">Welcome <%= @name %> ! 😊</small></h2>
<br>

<h5>Voici nos gossips les plus 🔥 :</h5><br>
<body>
  <div class="container">
    <% Gossip.all.each do |gossip| %>
    <div class="card-deck-wrapper"> 
      <div class="card-deck">
        <div class="card bg-secondary mb-3" style="width: 15rem;">
          <div class="card-header"><%=gossip.title %></div>
          <div class="card-body">
            <h6 class="card-text">Créé par <%=gossip.user.first_name %></h6>
            <p><%= link_to("Gossip link", "/gossips/#{gossip.id}") %></p>
          </div>
        </div>
      </div>
      <%  end  %>
    </div>
  </div>
</body>
</html>
